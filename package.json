{
  "name": "trainer-attendance",
  "version": "1.0.0",
  "description": "Backend for the trainer attendance app",
  "main": "lib/index.js",
  "scripts": {
    "compile": "babel --presets es2015 src -d lib",
    "prepublish": "npm run compile",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run compile && node lib/index.js"
  },
  "author": "Sander Geraedts",
  "license": "ISC",
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "body-parser": "^1.18.2",
    "express": "^4.16.3",
    "jsonwebtoken": "^8.2.1",
    "lodash": "^4.17.10",
    "mongoose": "^5.0.17",
    "mongoose-deep-populate": "^3.1.1",
    "sha1": "^1.1.1"
  },
  "engines": {
    "node": "8.9.4"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  },
  "nodemonConfig": {
    "ignore": [
      "lib/*"
    ]
  }
}
